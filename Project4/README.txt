1. 
In the keyword search page, it will display no results if there is no such keyword or empty query.
In the itemId search page, it will redirect to a reInputItemId.html if there is no valid itemId input including empty query.

2. 
In google map searching, if the item has latitude and longitude, I would first use its coordinate to search. If no, I then use its location to search. 

3.
By default, in the key world search page, I display only at most 20 results each page.